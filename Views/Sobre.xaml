<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiAppHotel.Views.Sobre"
             Title="Sobre"
             BackgroundColor="{StaticResource White}">

    <ScrollView>
        <VerticalStackLayout Padding="30"
                             VerticalOptions="CenterAndExpand"
                             Spacing="25">

            

            <Frame BackgroundColor="{StaticResource Gray100}"
                   CornerRadius="12"
                   Padding="15"
                   HasShadow="True">
                <Label Text="Descubra as melhores opções de hospedagem no litoral com o nosso app! Encontre hotéis incríveis, compare preços e garanta sua reserva de forma rápida, segura e prática. Aproveite suas férias com conforto e tranquilidade à beira-mar!"
                       FontSize="16"
                       FontFamily="PoiretOneRegular"
                       TextColor="{StaticResource Tertiary}"
                       HorizontalOptions="Center"
                       HorizontalTextAlignment="Center" />
            </Frame>

            <Frame BackgroundColor="{StaticResource Gray100}"
                   CornerRadius="12"
                   Padding="15"
                   HasShadow="True">
                <Label Text="Informações de Contato:&#10;📞 (11) 993-868-748&#10;📧 amarpousada@outlook.com"
                       FontSize="16"
                       FontFamily="PoiretOneRegular"
                       TextColor="{StaticResource Tertiary}"
                       HorizontalOptions="Center"
                       HorizontalTextAlignment="Center" />
            </Frame>
            <Label Text="Desenvolvedora: Isabella Mariano"
       FontSize="12"
       FontAttributes="Bold"
       FontFamily="PoiretOneRegular"
       TextColor="{StaticResource Tertiary}"
       HorizontalOptions="Center" />
            
            <Label Text="Ano de desenvolvimento: 2025"
                   FontSize="12"
                   FontFamily="PoiretOneRegular"
                   TextColor="{StaticResource Tertiary}"
                   HorizontalOptions="Center" />

            <Button Text="Voltar"
                    FontFamily="PoiretOneRegular"
                    Clicked="OnVoltarClicked"
                    BackgroundColor="{StaticResource Tertiary}"
                    TextColor="White"
                    CornerRadius="10"
                    Padding="10"
                    WidthRequest="150"
                    HorizontalOptions="Center" />
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
